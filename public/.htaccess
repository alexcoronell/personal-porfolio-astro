<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{HTTPS} !=o
    Header always set Content-Security-Policy "upgrade-insecure-requests;"
    RewriteCond %{HTTP_HOST} !^alexcoronell.dev$
    RewriteRule ^(.*)$ https://alexcoronell.dev/$1 [R=301,L]
</IfModule>